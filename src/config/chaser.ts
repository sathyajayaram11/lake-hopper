export const CATCH_RADIUS_M = 2.5; // chaser within this distance + a stumble while already Stumbling = Caught
export const CHASER_CLOSE_ON_STUMBLE_M = 3; // meters the chaser closes when the player stumbles
export const CHASER_RECOVER_M_PER_S = 1.5; // meters/second the chaser falls back when the player runs clean
