export const STUMBLE_GRACE_MS = 600; // how long Stumbling lasts before returning to Running, absent a second hit
export const HIT_WINDOW_M = 1.5; // a hazard within this distance counts as "at the player" this tick
export const SLOW_EFFECT_DURATION_MS = 2000;
export const SLOW_EFFECT_SPEED_FACTOR = 0.5;
